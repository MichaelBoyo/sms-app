decoded:
    cache:
        port: ${REDIS_PORT}
        host: ${REDIS_HOST}
        password: ${REDIS_PASSWORD}

spring:
    data:
        mongodb:
            uri: ${DB_URI}
            database: ${DB}

sms:
    api-key: ${SMS_API_KEY}
